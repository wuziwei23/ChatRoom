# TCP 多人聊天室 · 简易设计文档

## 1. 项目目标

在 Linux 环境下，使用 C 语言、TCP Socket 和多线程实现一个终端聊天室。

基本效果：

- 启动一个服务端，再启动多个客户端。
- 用户在客户端终端输入文字，按回车发送。
- 服务端收到消息后，转发给其他已连接的客户端。
- 其他客户端直接在各自终端上显示收到的消息。

当前只考虑基础群聊，不涉及图形界面、账号登录、数据库、私聊等功能。本文只描述框架和实现思路。

## 2. 整体架构

```text
客户端 A ──┐
           │
客户端 B ──┼── TCP 连接 ── 服务端
           │               │
客户端 C ──┘          接收并转发消息
```

一个服务端进程对应多个客户端进程，每个客户端与服务端保持一条 TCP 连接。客户端之间不直接连接。

例如：A 输入“你好”，服务端收到后发给 B 和 C，B、C 的终端显示“你好”。服务端不再把这条消息发回 A。

## 3. 服务端设计

### 3.1 主线程：负责接受连接

主线程的工作顺序：

1. 创建监听 Socket。
2. 绑定 IP 地址和端口。
3. 开始监听。
4. 等待客户端连接。
5. 连接成功后，把客户端加入列表，并创建一个处理线程。
6. 继续等待下一个客户端。

对应的主要接口是 socket、bind、listen、accept 和 pthread_create。

**监听 Socket** 用来接受新连接；accept 返回的 **客户端 Socket** 用来与某一个客户端收发消息，两者用途不同。

### 3.2 客户端处理线程：负责接收和广播

每连接一个客户端，就分配一个线程：

1. 等待这个客户端发来消息。
2. 收到完整的一行后，遍历客户端列表。
3. 把这行消息发送给除发送者以外的其他客户端。
4. 回到接收步骤，继续等待。
5. 客户端断开后，从列表中移除连接，关闭 Socket，结束线程。

例如，A、B、C 同时在线时，服务端有一个主线程和三个客户端处理线程。

### 3.3 客户端列表与互斥锁

使用一个定长数组保存客户端 Socket 即可，例如最多容纳 32 个客户端。空槽位用 -1 表示，不需要链表或复杂的连接对象。

多个处理线程会共同访问这个数组，因此使用一个互斥锁保护：

- 添加客户端时加锁。
- 广播时加锁，遍历数组并完成发送，再解锁。
- 删除客户端和关闭对应 Socket 时加锁。

这种方式容易理解，也能避免广播过程中连接被另一个线程关闭，或多线程同时向同一个客户端发送而导致消息交错。

它的局限是：某个客户端接收很慢时，广播可能阻塞，其他线程也会等待这把锁。当前作为少量客户端的学习版本，可以先接受这一限制。

## 4. 客户端设计

客户端先创建 Socket，通过 connect 连接服务端。连接成功后，用两个线程分别处理终端输入和网络接收。

| 线程 | 工作 |
|------|------|
| 主线程 | 从终端读取一行文字，发送给服务端 |
| 接收线程 | 等待服务端消息，将收到的内容打印到终端 |

这样，用户没有输入文字时，客户端也能及时显示其他人发来的消息。

第一版直接显示收到的文字即可，不需要复杂的终端界面。异步消息可能打断正在输入的文字显示，这不影响基础通信。

## 5. 消息如何发送和接收

### 5.1 使用普通文本

一行文字就是一条消息，以换行符作为结束标志，不设计额外的命令和消息类型。

为了区分发送者，初期可以手动输入“张三：你好”，不必实现昵称管理。

### 5.2 注意 TCP 没有消息边界

TCP 传输的是字节流：发送一行文字，接收方可能分几次收到；连续发送两行，也可能一次收到。

因此，服务端不能直接把“一次 recv”当作“一条完整消息”。简单处理方式：

1. 把收到的内容暂存到缓冲区。
2. 找到换行符后，取出完整的一行进行广播。
3. 同一次接收中有多行，就逐行处理。
4. 不足一行的内容留到下次接收。

客户端只需把接收到的字节按顺序输出到终端，原有换行符会自然分隔消息。

缓冲区可以先设为 1024 字节，并约定单条消息不能超过缓冲区容量。遇到超长输入要明确拒绝，避免越界。

### 5.3 发送可能需要多次完成

send 可能只发送一部分内容。因此发送时检查返回值，继续发送剩余部分，直到整条消息发完或发生错误。

## 6. 退出和错误处理

基础版本只需要处理以下情况：

- **连接失败**：提示错误并退出客户端。
- **客户端断开**：服务端 recv 返回 0 或出现连接错误，移除该客户端并释放资源。
- **发送失败**：停止向失效连接发送，由对应处理线程完成清理；避免多个线程重复关闭同一个 Socket。
- **服务端关闭**：客户端接收线程检测到断线，提示连接已关闭并结束客户端进程。
- **线程创建失败**：撤销刚加入列表的连接，并关闭 Socket。
- **人数达到上限**：关闭新连接，服务端打印提示。

先使用 Ctrl+C 结束程序，由操作系统回收进程资源，不增加退出命令和复杂的停服流程。服务端处理线程结束后应能自动回收线程资源，可采用 detached 线程。

另外，Linux 下向已断开的连接发送数据可能产生 SIGPIPE，需要忽略该信号或使用 MSG_NOSIGNAL，避免一个客户端断开导致整个服务端退出。

## 7. 建议的实现顺序

1. **先做单客户端连接**：服务端接受连接，客户端发送文字，服务端终端打印。
2. **再实现双向通信**：服务端回复文字，客户端能够接收并显示。
3. **给服务端加入多线程**：多个客户端可以同时连接，各自有独立处理线程。
4. **加入客户端列表和广播**：一个客户端发消息，其他客户端都能收到。
5. **给客户端加入接收线程**：终端输入和接收消息同时进行。
6. **补上必要的边界处理**：消息分行、发送不完整、断线、数组上限与互斥锁。

验证时打开三个以上终端：一个运行服务端，另外几个运行客户端。检查能否互相收发、关闭一个客户端后其他人能否继续聊天，以及新客户端能否再次加入。
